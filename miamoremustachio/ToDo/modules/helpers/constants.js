const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
};

const DEFAULT_SORT_ORDER = 'ascending';
const START_MESSAGE = 'ToDo server is running.';

module.exports = {
  ROLES,
  DEFAULT_SORT_ORDER,
  START_MESSAGE,
};